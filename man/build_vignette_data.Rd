% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vig_builder.R
\name{build_vignette_data}
\alias{build_vignette_data}
\title{Build vignette data with automatic multi-language detection}
\usage{
build_vignette_data(
  factors,
  design_df = NULL,
  vig_n = NULL,
  lang = NULL,
  prefix = "vig",
  text = NULL,
  names = NULL,
  store_design_data = TRUE
)
}
\arguments{
\item{factors}{Named list of factor definitions with multilingual labels}

\item{design_df}{Optional data frame with design matrix (columns: deck, vig,
factor variables coded as integers)}

\item{vig_n}{Number of vignettes per deck (required if design_df is NULL)}

\item{lang}{Single language code or NULL for auto-detection of all languages}

\item{prefix}{Prefix for vignette variable names (default: "vig")}

\item{text}{Optional named list of text templates by language}

\item{names}{Optional nested list structure for person names}

\item{store_design_data}{Logical, wheter to store labeled design data for preview}
}
\value{
List with structure:
\itemize{
\item vigs: number of vignettes
\item decks: number of decks
\item prefix: variable prefix
\item vig_vars: character vector of variable names
\item conditions: named list where each element is a list of language codes
(e.g., conditions$vigper1name$de, conditions$vigper1name$en)
\item texts: named list by language code (e.g., text$de, text$en)
\item design_data: (if store_design_data=TRUE) nested list by language with
labeled design data for creating previews
}
}
\description{
Build vignette data with automatic multi-language detection
}
\examples{
\dontrun{
# Auto-detect all languages
result <- build_vignette_data(
  factors = fct_per,
  design_df = design_sas,
  text = text_templates,
  names = names_5,
)

# Access structure:
result$conditions$vigper1name$de  # German condition string
result$conditions$vigper1name$en  # English condition string
result$texts$vigper1$de            # German vignette text for vignette 1
result$texts$vigper1$en            # English vignette text for vignette 1
}

}
